{
  "name": "naotrades-dashboard",
  "version": "1.0.0",
  "description": "NaoTrades premium dashboard override workspace",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp -r images scripts styles index.html dist/"
  },
  "private": true
}
